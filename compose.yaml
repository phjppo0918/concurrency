services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=concurrency'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=1234'
      - 'MYSQL_USER=myuser'
    ports:
      - '3306'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
